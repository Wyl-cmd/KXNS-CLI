# KXNS CLI Tools

## Guidelines

- Except for `Task` tool, tools should not refer to any types in `kxns_cli/wire/`. When importing things like `ToolReturnValue`, `DisplayBlock`, import from `kosong.tooling`.
